// export function getInfo(appName: string, runner: Runner): GameInfo | SideloadGame {
export function getInfo(launch: any, runner: any): any {
  // if (runner === 'sideload') {
  //   return getAppInfo(appName)
  // }
  // const game = getGame(appName, runner)
  // return game.getGameInfo()
  return {}
}
